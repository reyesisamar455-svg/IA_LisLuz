local VirtualUser = juego:GetService("VirtualUser")

juego:GetService("Players").LocalPlayer.Idled:Connect(function()
    Usuario virtual:Controlador de captura()
    Usuario virtual:ClickButton2(Vector2.new())
fin)
